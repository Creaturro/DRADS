---
// No props needed for this custom Hero Section
---

<section class="relative flex flex-col items-center px-8" style="margin: 0; padding: 0; margin-top: 50px; min-height: 1036px;">
  <!-- Centered Background Image Container -->
  <div 
    class="relative w-full max-w-[1920px] bg-center bg-cover bg-no-repeat flex flex-col items-center justify-center gap-4"
    style="background-image: url('/images/hero/Background.png'); aspect-ratio: 1920/838; min-height: 400px; margin-top: -50px;"
  >
    <!-- Letter images positioned above background - spelling DRADS -->
    <div class="flex items-center justify-center gap-4" style="transform: translateY(360px);">
      <img 
        src="/images/hero/hero-letters-_01.png" 
        alt="D - Letter 1" 
        class="w-auto h-auto"
      />
      <img 
        src="/images/hero/hero-letters-_02.png" 
        alt="R - Letter 2" 
        class="w-auto h-auto"
      />
      <img 
        src="/images/hero/hero-letters-_03.png" 
        alt="A - Letter 3" 
        class="w-auto h-auto"
      />
      <img 
        src="/images/hero/hero-letters-_04.png" 
        alt="S - Letter 4" 
        class="w-auto h-auto"
      />
      <img 
        src="/images/hero/hero-letters-_05.png" 
        alt="D - Letter 5" 
        class="w-auto h-auto"
      />
    </div>

    <!-- Main Content - Two Column Layout - Now in normal flow -->
    <div class="flex items-center justify-center w-full mt-[340px]">
      <div class="max-w-6xl mx-auto w-full">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 items-start">
          <!-- Left Column - Subtitle (mobile: below H1, desktop: left) -->
          <div class="lg:pr-8 order-2 md:order-1 mt-6 md:mt-0">
            <p class="font-h3 text-left md:text-right" id="hero-subtitle">
              We have the people, tools and procedures to develop custom software quickly and with high quality.
            </p>
            <style>
              @media (min-width: 768px) {
                #hero-subtitle {
                  line-height: 2 !important;
                }
              }
            </style>
          </div>

          <!-- Right Column - Main Heading (mobile: above subtitle, desktop: right) -->
          <div class="text-left order-1 md:order-2">
            <h1 class="font-h1">
              DELIGO<br />
              RAPID<br />
              APP<br />
              DEVELOPMENT<br />
              SYSTEM
    </h1>
          </div>
        </div>
      </div>
    </div>

    <!-- Centered Icon and Description - Now inside background image container, always below main content -->
    <div class="mx-auto w-full max-w-[1170px] mt-12 md:mt-0">
      <div class="flex justify-center m-4">
        <button
          id="scroll-button"
          class="mx-auto cursor-pointer bg-transparent border-none p-0 transition-all duration-200 hover:brightness-100"
          style="width: 33px; height: 59px; filter: brightness(0) invert(1) sepia(0) saturate(0) brightness(0.93);"
        >
          <img 
            src="/images/hero/scroll.png" 
            alt="Navigation icon" 
            class="w-full h-full"
            style="width: 33px; height: 59px; pointer-events: none;"
          />
        </button>
      </div>
      <p class="font-h3 text-left" style="margin-top: 48px;">
        We've been involved in building software for 18 years. The DRADS work system is the result of 
        our accumulated experience combined with modern tools and approaches. We took the whole 
        software development process apart, looked at every piece and combined together for a 
        comprehensive, fast and efficient way to build custom software for businesses of all sizes.
      </p>
    </div>
  </div>
</section>

<!-- Add Google Fonts -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300;0,400;0,600;0,700;1,300;1,400&family=Montserrat:wght@400;700;800&display=swap" rel="stylesheet">

<script>
  // Handle scroll button click
  document.getElementById('scroll-button')?.addEventListener('click', () => {
    window.scrollBy({
      top: 400,
      behavior: 'smooth'
    });
  });

  // Handle hover effects for scroll button
  const scrollButton = document.getElementById('scroll-button');
  if (scrollButton) {
    scrollButton.addEventListener('mouseenter', () => {
      scrollButton.style.filter = 'brightness(0) invert(1)';
    });
    
    scrollButton.addEventListener('mouseleave', () => {
      scrollButton.style.filter = 'brightness(0) invert(1) sepia(0) saturate(0) brightness(0.93)';
    });
  }
</script>
